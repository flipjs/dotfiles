name: backbone
root: ~/Documents/projects/local-server

windows:
  - neo:
      layout: main-vertical
      panes:
        - nvm use --delete-prefix $NODE_BACKBONE_VERSION && npm config delete prefix && npm config set prefix $NVM_DIR/versions/node/$NODE_BACKBONE_VERSION && figlet neo | lolcat && node -v | cowsay -f dragon-and-cow | lolcat
        - nvm use --delete-prefix $NODE_BACKBONE_VERSION && npm config delete prefix && npm config set prefix $NVM_DIR/versions/node/$NODE_BACKBONE_VERSION && figlet node | lolcat && node -v | cowsay | lolcat
        - nvm use --delete-prefix $NODE_BACKBONE_VERSION && npm config delete prefix && npm config set prefix $NVM_DIR/versions/node/$NODE_BACKBONE_VERSION && fortune | cowsay | lolcat
  - morpheus:
      layout: main-vertical
      panes:
        - nvm use --delete-prefix $NODE_BACKBONE_VERSION && npm config delete prefix && npm config set prefix $NVM_DIR/versions/node/$NODE_BACKBONE_VERSION && figlet morpheus | lolcat && node -v | cowsay -f dragon-and-cow | lolcat
        - nvm use --delete-prefix $NODE_BACKBONE_VERSION && npm config delete prefix && npm config set prefix $NVM_DIR/versions/node/$NODE_BACKBONE_VERSION && figlet node | lolcat && node -v | cowsay | lolcat
        - nvm use --delete-prefix $NODE_BACKBONE_VERSION && npm config delete prefix && npm config set prefix $NVM_DIR/versions/node/$NODE_BACKBONE_VERSION && fortune | cowsay | lolcat
  - trinity:
      layout: main-vertical
      panes:
        - nvm use --delete-prefix $NODE_BACKBONE_VERSION && npm config delete prefix && npm config set prefix $NVM_DIR/versions/node/$NODE_BACKBONE_VERSION && figlet trinity | lolcat && node -v | cowsay -f dragon-and-cow | lolcat
        - nvm use --delete-prefix $NODE_BACKBONE_VERSION && npm config delete prefix && npm config set prefix $NVM_DIR/versions/node/$NODE_BACKBONE_VERSION && figlet node | lolcat && node -v | cowsay | lolcat
        - nvm use --delete-prefix $NODE_BACKBONE_VERSION && npm config delete prefix && npm config set prefix $NVM_DIR/versions/node/$NODE_BACKBONE_VERSION && fortune | cowsay | lolcat
  - cypher:
      layout: main-vertical
      panes:
        - nvm use --delete-prefix $NODE_BACKBONE_VERSION && npm config delete prefix && npm config set prefix $NVM_DIR/versions/node/$NODE_BACKBONE_VERSION && figlet cypher | lolcat && node -v | cowsay -f dragon-and-cow | lolcat
        - nvm use --delete-prefix $NODE_BACKBONE_VERSION && npm config delete prefix && npm config set prefix $NVM_DIR/versions/node/$NODE_BACKBONE_VERSION && figlet node | lolcat && node -v | cowsay | lolcat
        - nvm use --delete-prefix $NODE_BACKBONE_VERSION && npm config delete prefix && npm config set prefix $NVM_DIR/versions/node/$NODE_BACKBONE_VERSION && fortune | cowsay | lolcat
