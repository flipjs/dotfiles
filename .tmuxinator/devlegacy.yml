name: legacy
root: ~/Documents/projects/local-server

windows:
  - cypher:
      layout: main-vertical
      panes:
        - nvm use $NODE_OLD_VERSION && figlet backbourne legacy | lolcat && node -v | cowsay -f dragon-and-cow | lolcat
        - nvm use $NODE_OLD_VERSION && figlet cypher | lolcat && node -v | cowsay | lolcat
        - nvm use $NODE_OLD_VERSION && fortune | cowsay | lolcat
  - tank:
      layout: main-vertical
      panes:
        - nvm use $NODE_OLD_VERSION && figlet backbourne legacy | lolcat && node -v | cowsay -f dragon-and-cow | lolcat
        - nvm use $NODE_OLD_VERSION && figlet tank | lolcat && node -v | cowsay | lolcat
        - nvm use $NODE_OLD_VERSION && fortune | cowsay | lolcat
  - niobe:
      layout: main-vertical
      panes:
        - nvm use $NODE_OLD_VERSION && figlet backbourne legacy | lolcat && node -v | cowsay -f dragon-and-cow | lolcat
        - nvm use $NODE_OLD_VERSION && figlet niobe | lolcat && node -v | cowsay | lolcat
        - nvm use $NODE_OLD_VERSION && fortune | cowsay | lolcat
  - link:
      layout: main-vertical
      panes:
        - nvm use $NODE_OLD_VERSION && figlet backbourne legacy | lolcat && node -v | cowsay -f dragon-and-cow | lolcat
        - nvm use $NODE_OLD_VERSION && figlet link | lolcat && node -v | cowsay | lolcat
        - nvm use $NODE_OLD_VERSION && fortune | cowsay | lolcat
