name: legacy
root: ~/Documents/projects/local-server

windows:
  - neo:
      layout: main-vertical
      panes:
        - nvm use --delete-prefix $NODE_OLD_VERSION && npm config delete prefix && figlet neo | lolcat && node -v | cowsay -f dragon-and-cow | lolcat
        - nvm use --delete-prefix $NODE_OLD_VERSION && npm config delete prefix && figlet neo | lolcat && node -v | cowsay | lolcat
        - nvm use --delete-prefix $NODE_OLD_VERSION && npm config delete prefix && fortune | cowsay | lolcat
  - morpheus:
      layout: main-vertical
      panes:
        - nvm use --delete-prefix $NODE_OLD_VERSION && npm config delete prefix && figlet morpheus | lolcat && node -v | cowsay -f dragon-and-cow | lolcat
        - nvm use --delete-prefix $NODE_OLD_VERSION && npm config delete prefix && figlet morpheu | lolcat && node -v | cowsay | lolcat
        - nvm use --delete-prefix $NODE_OLD_VERSION && npm config delete prefix && fortune | cowsay | lolcat
  - trinity:
      layout: main-vertical
      panes:
        - nvm use --delete-prefix $NODE_OLD_VERSION && npm config delete prefix && figlet trinity | lolcat && node -v | cowsay -f dragon-and-cow | lolcat
        - nvm use --delete-prefix $NODE_OLD_VERSION && npm config delete prefix && figlet trinity | lolcat && node -v | cowsay | lolcat
        - nvm use --delete-prefix $NODE_OLD_VERSION && npm config delete prefix && fortune | cowsay | lolcat
  - cypher:
      layout: main-vertical
      panes:
        - nvm use --delete-prefix $NODE_OLD_VERSION && npm config delete prefix && figlet cypher | lolcat && node -v | cowsay -f dragon-and-cow | lolcat
        - nvm use --delete-prefix $NODE_OLD_VERSION && npm config delete prefix && figlet cypher | lolcat && node -v | cowsay | lolcat
        - nvm use --delete-prefix $NODE_OLD_VERSION && npm config delete prefix && fortune | cowsay | lolcat
